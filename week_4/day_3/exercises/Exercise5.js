class Labrador extends Dog {
    constructor(name, size) {
      super(name);  // Appel du constructeur parent pour initialiser 'name'
      this.size = size;  // Ajout de la propriété 'size'
    }
  }